spring.application.name=auth

spring.profiles.active=dev
server.port=8080

# Load secrets from .env file (gitignored) — falls back to OS environment variables if .env absent
spring.config.import=optional:file:./.env[.properties]