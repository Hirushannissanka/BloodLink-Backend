# Copy this file to .env and fill in the values
# .env is gitignored — never commit the real secrets

# Shared JWT secret across all BloodLink microservices (base64-encoded 256-bit key)
JWT_SECRET=your_shared_base64_secret_here
